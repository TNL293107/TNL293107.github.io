export interface TechIcon {
  readonly name: string;
  /** Official brand colour, as published by Simple Icons. */
  readonly brand: string;
  /** Path data on a 24x24 viewBox. */
  readonly path: string;
}
